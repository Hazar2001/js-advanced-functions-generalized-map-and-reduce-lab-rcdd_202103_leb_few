// Add your functions here
function map(Array){

  function map1(Array){
      return Array.map(x => x * 2);
  }

  function map2(Array){
      return Array.map(x => x * x);
  }

  function map3(Array){
      return Array.map(x => x * x);
  }


return [map1(),map2(),map3()];
}
